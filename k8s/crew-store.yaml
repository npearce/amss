apiVersion: apps/v1
kind: Deployment
metadata:
  name: crew-store
  namespace: amss
  labels:
    app.kubernetes.io/name: crew-store
    app.kubernetes.io/part-of: amss
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: crew-store
  template:
    metadata:
      labels:
        app.kubernetes.io/name: crew-store
        app.kubernetes.io/part-of: amss
    spec:
      containers:
      - name: crew-store
        image: amss/crew-store:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8083
        env:
        - name: PORT
          value: "8083"
        - name: SEED_PATH
          value: /seed-data/crew.json
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 2
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            memory: 64Mi
            cpu: 100m
          limits:
            memory: 128Mi
            cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: crew-store
  namespace: amss
  labels:
    app.kubernetes.io/name: crew-store
    app.kubernetes.io/part-of: amss
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: crew-store
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
