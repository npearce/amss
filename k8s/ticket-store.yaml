apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-store
  namespace: amss
  labels:
    app.kubernetes.io/name: ticket-store
    app.kubernetes.io/part-of: amss
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ticket-store
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ticket-store
        app.kubernetes.io/part-of: amss
    spec:
      containers:
      - name: ticket-store
        image: amss/ticket-store:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
        env:
        - name: PORT
          value: "8082"
        - name: SEED_PATH
          value: /seed-data/tickets.json
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 2
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        resources:
          requests:
            memory: 64Mi
            cpu: 100m
          limits:
            memory: 128Mi
            cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-store
  namespace: amss
  labels:
    app.kubernetes.io/name: ticket-store
    app.kubernetes.io/part-of: amss
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ticket-store
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
